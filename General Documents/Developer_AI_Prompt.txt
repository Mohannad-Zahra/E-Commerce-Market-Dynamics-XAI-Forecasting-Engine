ACT as an Expert Python Web Scraping Engineer.

I am a developer assigned to handle retailer integrations for our team's Distributed Web Scraping Infrastructure. 

BACKGROUND CONTEXT:
Our infrastructure is 100% built. It handles cloud uploading to Google Drive, SQLite database backups, crash-recovery, global UTC scheduling, legally-compliant rate-limiting, and SMTP notifications automatically. 

My specific job is to build the "ScraperPayload" subclass for a new retailer. 
The website I am assigned to scrape is: [INSERT RETAILER NAME AND URLS HERE]

YOUR GOAL is to write the complete payload python module for me.

WHERE TO FIND THE ARCHITECTURE CONTEXT:
Before writing any code, I want you to read the following files in my workspace to fully understand our architecture and constraints:
1. Read `General Documents/architecture.md` (Focus on Section 2.2 Layer 2 regarding Scraper Payloads).
2. Read `Web Scrapper/payload/base.py` (This is the abstract interface you must inherit from and it contains the `validate_record` schema).
3. Read `Web Scrapper/payload/dummy_retailer.py` (This is the boilerplate template to see how exactly we extract records and implement the class).
4. (Optional) Read `Web Scrapper/payload/twob.py` if you need a real-world example of Playwright AJAX-pagination and lazy loading.

RULES FOR YOUR IMPLEMENTATION:
1. Create a class named `Scraper` that inherits from `payload.base.ScraperPayload`.
2. The class MUST implement an `async def run(self) -> list[dict]:` method.
3. Every dictionary in the returned list MUST contain exactly the 8 schema fields defined in the Bronze-layer schema you read from `base.py` (including `category` and `sub_category`).
4. DO NOT write `time.sleep()` for rate limiting between page loads. The global engine (`scraper.py`) handles the hardcoded 5-7s rate limits automatically. If you need navigation waits, use Playwright's native `await page.wait_for_timeout()`.
5. You must use `async_playwright` (or `httpx` + `BeautifulSoup` if Playwright is overkill, but Playwright is required for dynamic JS sites).
6. Handle pagination robustly! If using Playwright, write logic to scroll down the page incrementally before checking for "Next" buttons.
7. Write a corresponding `pytest` test script to be placed in the `tests/` folder (e.g. `tests/test_[retailer].py`) that mocks the extraction logic and verifies the output records pass the 8-field Bronze schema.

8. CONFIGURATION MANAGEMENT: 
   - You MUST instruct the developer to update `config/config.json` to suit their environment (SMTP credentials, Google Drive folder IDs, etc.).
   - You MUST proactively ask the developer for their specific requirements (e.g., target URLs, categories, email recipients).
   - List these requirements clearly in the chat so the developer can provide them. Once provided, update the `config.json` file accordingly.

9. DOCUMENTATION MAINTENANCE:
   - You are responsible for the integrity of the project's documentation.
   - Whenever you create a new module, modify an existing one, or change the system architecture, you MUST immediately update all relevant `.md` documentation files (e.g., `architecture.md`, `proposal.md`, etc.) to reflect the current state of the engine.

Please read those files first, let me know when you understand the architecture, and provide the complete robust Python code for my retailer payload AND its test file!
